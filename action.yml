name: 'Add field option to Height App'
description: 'Add field option to Height App'
branding:
  icon: check-square
  color: green
inputs:
  field:  # id of the field to add option
    description: 'Field ID to add an option'
    required: true
  option:
    description: 'Option to add'
    required: true
outputs:
  status:
    description: 'Status of the action'
runs:
  using: 'node12'
  main: 'index.js'
